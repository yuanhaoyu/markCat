{
  "name": "markcat",
  "version": "0.0.1",
  "description": "chrome extensions",
  "main": "index.js",
  "scripts": {
    "dev": "cd app && npm run serve",
    "install": "cd app && npm install",
    "build": "cd app && npm run build",
    "pack": "rm -rf dist && npm run build && cp -r app/dist dist && cp manifest.json dist/manifest.json && cp cat-dev.png dist/cat-dev.png",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yuanhaoyu/markCat.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/yuanhaoyu/markCat/issues"
  },
  "homepage": "https://github.com/yuanhaoyu/markCat#readme",
  "dependencies": {
  }
}
